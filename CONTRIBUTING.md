# Contributing

## 브랜치 전략

Git Flow 전략을 기반으로 합니다.

| 브랜치 | 설명 |
| --- | --- |
| `main` | 최종 배포 브랜치 |
| `develop` | 개발 통합 브랜치 (PR로만 머지) |
| `feature-{티켓ID}` | 기능 개발 브랜치 |
| `hotfix-{티켓ID}` | 라이브에서 발생한 버그 긴급 수정 브랜치 |

### 브랜치 네이밍 예시

```
feature-QVE-4-1-1
hotfix-QVE-4-2-1
restore-QVE-4-3-1
```

---

## 커밋 컨벤션

### 커밋 메시지 형식

```
{작업종류} : {티켓ID}({작업내용})
```

### 작업 종류

| 키워드 | 설명 |
| --- | --- |
| `feat` | 새로운 기능 개발 |
| `fix` | 기존 기능 수정 |
| `doc` | 문서 수정 (Swagger, README 등) |
| `chore` | 설정, 환경 세팅 등 |
| `refactor` | 동작 변경 없는 리팩터링 |
| `test` | 테스트 관련 작업 |

### 커밋 메시지 예시

```
feat : QVE-4-1-1(아이디 중복확인 API 개발)
fix : QVE-4-2-1(이메일 인증번호 발송 오류 수정)
doc : QVE-4-3-1(이메일 인증 API Swagger 명세 추가)
chore : 프로젝트 초기 세팅
refactor : QVE-4-5-1(회원가입 로직 리팩터링)
```

### 커밋 단위

- 커밋 최소 단위는 **티켓 단위**

---

## Pull Request 규칙

- 머지는 **무조건 PR 기반**으로 진행
- PR은 **develop 브랜치**로 올리기
- PR 제목은 커밋 메시지 형식과 동일하게 작성
- 머지 전 **팀원 1명 이상 리뷰** 권장

### PR 제목 예시

```
feat : QVE-4-1-1(아이디 중복확인 API 개발)
```
